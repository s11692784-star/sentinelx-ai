{
  "name": "sentinelx-frontend-moved",
  "private": true,
  "description": "MOVED to repository root. Clear Vercel Root Directory (do not use frontend/).",
  "scripts": {
    "postinstall": "echo \"ERROR: Next.js app moved to repo root. In Vercel Settings set Root Directory to empty (.) not frontend\" && exit 1",
    "build": "echo \"ERROR: Use repository root, not frontend/\" && exit 1"
  }
}
