# Moved

The Next.js app was moved to the **repository root** so Vercel can detect `next` automatically.

Do **not** set Vercel Root Directory to `frontend`.

Use repo root. See [/DEPLOY.md](../DEPLOY.md).
