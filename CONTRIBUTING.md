# Contributing

1. Fork & branch from `main`
2. Use conventional commits when possible
3. Never commit secrets (`.env`, keys, dumps)
4. Run backend tests and frontend `tsc` before PR
5. Keep tenant isolation on every new API route
