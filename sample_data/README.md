# Sample data

`leaky_config.env` uses placeholders safe for git.

For local demos that exercise full regex detection, generate fixtures yourself or use the in-app Secrets page sample payload (not committed with live-looking tokens).
